#timeline_popup.modal.fade("*ngIf"='condition', tabindex='-1', role='dialog')
    .modal-dialog(role='document')
        .modal-content
            .modal-header
                button.close(type='button', data-dismiss='modal', aria-label='Close')
                    span(aria-hidden='true') ×
                h4.modal-title(style='border-bottom: none') Encounter from {{condition.onsetDateTime | amTimeAgo}}
            .modal-body
                p {{condition.code.coding[0].display}}
                p
                    b Code System:
                    a(href='{{condition.code.coding[0].system}}') {{condition.code.coding[0].system}}
                p
                    b Code:
                    |  {{condition.code.coding[0].code}}
            .modal-footer
                button.btn.btn-default(type='button', data-dismiss='modal') Close
hoverBox(#hoverBox='')
